var sexo = "M",
  idade = 27;

if (sexo === "M" && idade >= 18) {
  console.log("Deve para entrada de 50$");
}

if (sexo === "F" || idade >= 18) {
  console.log("entrada gratuita");
}

if (sexo !== "F" && 18) {
  console.log("entra de graça");
}
// armazenar uma variavel

var sexo = "M";

var masculino = sexo === "M";

console.log(masculino);
